/**
 * PostCSS configuration for the Jet Stream Clean quote app.  This
 * configuration wires up Tailwind CSS and Autoprefixer to the build
 * pipeline used by Create React App.  You generally won't need to
 * modify this file unless you add additional PostCSS plugins.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
